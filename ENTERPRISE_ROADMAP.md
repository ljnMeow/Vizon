# Vizon 企业级 3D 组态编辑器 — 功能缺口分析与优先级计划

> 基于对 `packages/core`、`apps/server`、`apps/web` 三层的完整代码审查，对照企业级 3D 组态编辑器的标准能力模型，梳理缺失功能并按优先级排列。

---

## 现有能力概览

| 层 | 已有能力 |
|---|---|
| **Core** | ThreeEditor 主控、9 种内置几何体、2 种相机、6 种灯光、13 种材质类型切换、纹理/HDR/EXR 加载、后处理（Bloom/Outline）、Undo/Redo、单选/多选、Transform 控制器（平移/旋转/缩放）、VizonDocument 序列化/反序列化、视图预设动画、管路编辑（Conduit）、截图、事件总线、场景树管理 |
| **Server** | JWT 双令牌认证（含 Refresh Rotation）、用户 CRUD、场景 Bundle 上传/下载、纹理 CRUD（8 类分类、HDR 自动缩略图）、PostgreSQL + Redis、OpenAPI 文档、统一 JSON 响应格式 |
| **Web** | 登录页、设计页（3D 视口）、Inspector（属性/场景设置/材质/特效）、AssetPane（系统资产/用户资产/场景结构/历史记录）、Transform/视图预设工具栏、i18n、纹理选择器、颜色选择器 |

---

## P0 — 核心组态能力（无此项则产品不成立）

### 1. 数据绑定系统

组态编辑器的核心：将 3D 对象属性绑定到外部数据源，实现实时驱动的可视化。

| 子项 | 说明 |
|------|------|
| 1.1 数据源管理 | 定义数据源类型（REST API 轮询、WebSocket、MQTT、OPC UA），配置连接参数、认证方式、数据路径 |
| 1.2 绑定模型定义 | 设计 `DataBinding` schema：`{ targetUuid, propertyPath, sourceId, sourcePath, transform?, sampleInterval? }`，支持单向绑定和表达式转换 |
| 1.3 绑定编辑 UI | Inspector 面板中新增"数据绑定"tab，可视化配置对象属性与数据源的映射关系 |
| 1.4 运行时绑定引擎 | Core 层实现 `BindingEngine`，按 `sampleInterval` 拉取/接收数据，写入目标属性，触发重渲染 |
| 1.5 绑定状态可视化 | 编辑态下在对象旁显示当前绑定值和连接状态指示器（绿色/红色/灰色） |
| 1.6 序列化支持 | 将绑定配置写入 `VizonDocument.bindings`，支持导入导出 |

### 2. 事件/触发器系统

组态场景需要根据条件触发动作（报警、跳转、显隐、动画）。

| 子项 | 说明 |
|------|------|
| 2.1 事件类型定义 | 支持事件类型：点击事件、数据阈值事件、定时事件、状态变更事件 |
| 2.2 动作类型定义 | 支持动作类型：显隐对象、播放动画、切换材质/颜色、弹出面板、播放音效、发送控制指令 |
| 2.3 触发器编辑 UI | Inspector 或独立面板中配置"当 X 发生时执行 Y"规则，支持多条件 AND/OR 组合 |
| 2.4 运行时触发器引擎 | Core 层实现 `TriggerEngine`，监听事件源，评估条件，执行动作链 |
| 2.5 序列化支持 | 写入 `VizonDocument.triggers` |

### 3. 运行时播放器（Runtime Player）

编辑态与运行态分离：编辑器用于配置，播放器用于部署和展示。

| 子项 | 说明 |
|------|------|
| 3.1 播放器核心 | 独立于编辑器的轻量运行时，仅加载 VizonDocument + 绑定引擎 + 触发器引擎，无编辑器 UI 依赖 |
| 3.2 播放器页面路由 | Web 端新增 `/player/:sceneId` 路由，全屏 3D 渲染 + 数据驱动 |
| 3.3 播放器嵌入支持 | 提供 `<iframe>` 嵌入方案和 JS SDK，支持第三方系统嵌入播放器 |
| 3.4 全屏/缩放适配 | 播放器自适应容器尺寸，支持全屏模式 |
| 3.5 播放器性能优化 | 冻结场景树（`matrixAutoUpdate=false`）、按需渲染（无变化时不重绘）、LOD 支持 |

### 4. 2D 叠加层/仪表板系统

组态场景需要在 3D 画面上叠加 2D 信息面板（仪表盘、图表、数据表格）。

| 子项 | 说明 |
|------|------|
| 4.1 2D 面板数据模型 | 定义 `OverlayPanel`：位置（屏幕坐标/世界坐标锚定）、尺寸、组件列表 |
| 4.2 基础组件库 | 文本标签、数值显示、进度条、仪表盘、状态指示灯、简单折线图 |
| 4.3 面板编辑器 | 拖拽式面板布局编辑器，支持组件拖入、属性配置、数据绑定 |
| 4.4 运行时渲染 | 播放器中叠加 HTML/CSS 层渲染面板，数据更新实时刷新 |
| 4.5 序列化 | 写入 `VizonDocument.overlays` |

### 5. 项目管理页面

当前仅有登录页和设计页，缺少项目级管理入口。

| 子项 | 说明 |
|------|------|
| 5.1 项目列表页 | `/projects` 路由，展示用户所有场景的卡片/列表视图（缩略图、名称、修改时间） |
| 5.2 创建/导入项目 | 新建空项目、从模板创建、导入 VizonDocument 文件 |
| 5.3 项目删除/归档 | 删除确认、软删除/归档 |
| 5.4 项目搜索/筛选 | 按名称搜索、按时间/类型排序 |

---

## P1 — 编辑器增强（显著提升可用性）

### 6. 键盘快捷键系统

| 子项 | 说明 |
|------|------|
| 6.1 快捷键注册框架 | Core 层定义 `ShortcutRegistry`，支持命令 ID → 快捷键映射，冲突检测 |
| 6.2 标准快捷键集 | Ctrl+Z/Y 撤销重做、Ctrl+C/V/X 复制粘贴剪切、Delete 删除、Ctrl+D 复制、Ctrl+S 保存、Ctrl+A 全选、G/R/S 切换变换模式（仿 Blender） |
| 6.3 快捷键自定义 UI | 设置面板中允许用户重映射快捷键 |
| 6.4 快捷键提示 | 菜单项和工具提示中显示当前快捷键 |

### 7. 对齐与分布工具

| 子项 | 说明 |
|------|------|
| 7.1 对齐工具 | 左对齐/右对齐/居中/顶部对齐/底部对齐/垂直居中/水平居中（基于包围盒） |
| 7.2 分布工具 | 水平等间距/垂直等间距分布 |
| 7.3 对齐参考线 | 拖拽时显示与其他对象的对齐参考线（Snap Guides） |
| 7.4 网格吸附 | 对象变换时吸附到自定义网格间距 |

### 8. 自动保存与崩溃恢复

| 子项 | 说明 |
|------|------|
| 8.1 定时自动保存 | 每隔 N 秒自动保存到 localStorage（草稿）+ 服务器（可选） |
| 8.2 脏标记追踪 | 精确追踪文档是否有未保存变更，关闭/离开时提示 |
| 8.3 崩溃恢复 | 页面重新加载时检测 localStorage 中的草稿，提示恢复 |
| 8.4 版本快照 | 手动保存时在服务端保留历史版本（至少最近 10 个） |

### 9. 场景树增强

| 子项 | 说明 |
|------|------|
| 9.1 拖拽排序 | 场景树节点支持拖拽调整层级和顺序 |
| 9.2 右键菜单 | 重命名、删除、复制、粘贴、显隐切换、展开/折叠 |
| 9.3 搜索过滤 | 按名称/类型搜索场景树中的节点 |
| 9.4 图标类型区分 | 不同类型节点（相机/灯光/网格/组）使用不同图标 |
| 9.5 批量操作 | 多选后批量显隐、删除、移动 |

### 10. 更多 3D 格式支持

| 子项 | 说明 |
|------|------|
| 10.1 FBX 导入 | 通过 `fbx2gltf` 转换或 Three.js FBXLoader 直接加载 |
| 10.2 OBJ 导入 | OBJLoader + MTLLoader 支持 |
| 10.3 STL 导入 | STLLoader 支持，常用于工业 CAD 模型 |
| 10.4 模型预览 | 上传模型后在 AssetPane 中显示缩略图预览 |
| 10.5 服务端模型转换 | Server 端集成模型格式转换管线（FBX/OBJ → glTF），统一存储为 glTF |

### 11. 材质库与预设

| 子项 | 说明 |
|------|------|
| 11.1 内置材质预设 | 常用工业材质库（金属/塑料/玻璃/陶瓷/混凝土等 PBR 预设） |
| 11.2 用户自定义材质库 | 保存/加载用户创建的材质预设，含纹理映射 |
| 11.3 材质快速应用 | 场景树/对象右键 → "应用材质预设" |
| 11.4 材质缩略图 | 预设库中显示材质球预览渲染图 |

### 12. 测量与标注工具

| 子项 | 说明 |
|------|------|
| 12.1 距离测量 | 两点间距离标注，实时显示数值 |
| 12.2 角度测量 | 三点间角度标注 |
| 12.3 标注系统 | 可添加文本标注，关联到 3D 对象或世界坐标，支持引线 |
| 12.4 标注序列化 | 标注数据写入 VizonDocument，播放器中可选显示 |

---

## P2 — 协作与平台能力（团队和企业级需求）

### 13. 实时协作编辑

| 子项 | 说明 |
|------|------|
| 13.1 WebSocket 基础设施 | Server 端集成 Django Channels + Redis Channel Layer |
| 13.2 在线状态 | 显示当前在线协作者的头像和光标位置 |
| 13.3 操作同步 | 基于 OT/CRDT 的场景操作同步（选区/变换/属性变更） |
| 13.4 冲突解决 | 多人同时编辑同一对象时的冲突检测和解决策略 |
| 13.5 协作者光标 | 3D 视口中显示其他协作者的选择状态和视点 |

### 14. 权限与角色管理

| 子项 | 说明 |
|------|------|
| 14.1 角色模型 | 定义角色：管理员/编辑者/查看者，每个角色对应权限集 |
| 14.2 项目级权限 | 项目设置中配置成员和角色 |
| 14.3 组织/团队 | 用户归属组织，组织内共享资产和项目 |
| 14.4 分享链接 | 生成只读分享链接，支持密码保护和过期时间 |
| 14.5 操作审计日志 | 记录关键操作（创建/删除/分享/权限变更） |

### 15. 模板系统

| 子项 | 说明 |
|------|------|
| 15.1 模板定义 | 模板 = 预配置的 VizonDocument + 默认绑定配置 + 样板数据源 |
| 15.2 系统模板 | 内置行业模板（智慧工厂/智慧城市/能源监控/数据中心） |
| 15.3 用户自定义模板 | 从已有项目保存为模板，支持模板分享 |
| 15.4 从模板创建项目 | 项目创建流程中选择模板，自动填充场景和配置 |

### 16. 资产市场/共享库

| 子项 | 说明 |
|------|------|
| 16.1 公共资产库 | 系统提供的 3D 模型、材质、纹理、HDRI 等资产的浏览和下载 |
| 16.2 资产分类/标签 | 多维度分类（行业/类型/风格）+ 自由标签 |
| 16.3 资产版本管理 | 资产更新时保留版本，项目引用不受影响 |
| 16.4 团队资产共享 | 组织内资产共享和权限控制 |

---

## P3 — 高级渲染与交互（差异化竞争力）

### 17. 动画系统

| 子项 | 说明 |
|------|------|
| 17.1 关键帧动画 | 时间线面板，支持位置/旋转/缩放/材质属性的关键帧编辑 |
| 17.2 动画剪辑 | 支持多个动画片段的创建、编辑、命名 |
| 17.3 动画触发 | 组态事件触发动画播放（开/关/暂停/倒放） |
| 17.4 路径动画 | 对象沿指定路径运动的动画 |
| 17.5 相机动画 | 预设相机巡游路径，播放器中自动巡游 |
| 17.6 动画序列化 | 写入 VizonDocument.animations |

### 18. 高级渲染特性

| 子项 | 说明 |
|------|------|
| 18.1 SSAO | 屏幕空间环境光遮蔽 |
| 18.2 SSR | 屏幕空间反射 |
| 18.3 体积光 | God Rays / Volumetric Light 效果 |
| 18.4 自定义后处理管线 | 允许用户选择和排序后处理效果 |
| 18.5 渲染预设 | 低/中/高质量渲染预设，适配不同硬件 |

### 19. 粒子与特效系统

| 子项 | 说明 |
|------|------|
| 19.1 粒子发射器 | 位置/速度/颜色/生命周期等参数配置 |
| 19.2 预设粒子效果 | 烟雾/火焰/水滴/火花等预设 |
| 19.3 粒子触发 | 组态事件触发粒子效果 |

### 20. 物理与碰撞

| 子项 | 说明 |
|------|------|
| 20.1 物理引擎集成 | 基础刚体物理（Rapier.js 或 Cannon.js） |
| 20.2 碰撞体可视化 | 编辑态显示碰撞体轮廓 |
| 20.3 物理属性面板 | 质量/摩擦/弹性等参数配置 |

---

## P4 — 工程化与生产就绪

### 21. 部署与运维

| 子项 | 说明 |
|------|------|
| 21.1 Docker 化 | Dockerfile（Web/Server）+ docker-compose.yml（含 PostgreSQL/Redis） |
| 21.2 CI/CD 管线 | GitHub Actions：lint → test → build → deploy |
| 21.3 对象存储迁移 | Server 端支持 S3/OSS 存储后端，替代本地文件系统 |
| 21.4 CDN 配置 | 静态资源和纹理文件通过 CDN 分发 |
| 21.5 环境配置分离 | DEBUG/SECRET_KEY/ALLOWED_HOSTS 等必须通过环境变量注入 |

### 22. 安全加固

| 子项 | 说明 |
|------|------|
| 22.1 SECRET_KEY 外置 | 移除硬编码 SECRET_KEY，从环境变量读取 |
| 22.2 CORS 生产配置 | 根据环境动态配置 CORS 允许源 |
| 22.3 API 限流 | DRF Throttling 配置，防止接口滥用 |
| 22.4 文件上传校验 | 文件类型/大小白名单、病毒扫描（可选） |
| 22.5 HTTPS 强制 | 生产环境强制 HTTPS |
| 22.6 密码修改接口 | 实现 `/api/customers/{id}/password/` 端点 |
| 22.7 邮箱验证 | 注册后邮箱验证流程 |

### 23. 监控与可观测性

| 子项 | 说明 |
|------|------|
| 23.1 结构化日志 | 统一日志格式，支持 JSON 输出 |
| 23.2 APM 集成 | 请求追踪、慢查询监控（Sentry/Datadog） |
| 23.3 健康检查端点 | `/api/health/` 返回各依赖服务状态 |
| 23.4 前端错误上报 | 前端异常捕获和上报（Sentry SDK） |
| 23.5 性能指标 | 3D 视口帧率、内存占用的客户端指标采集 |

### 24. 测试覆盖

| 子项 | 说明 |
|------|------|
| 24.1 Core 单元测试 | 关键模块的测试覆盖（History、SceneGraph、Persistence） |
| 24.2 Server API 测试 | DRF APITestCase 覆盖所有端点 |
| 24.3 Web 集成测试 | 关键用户流程的 E2E 测试（Playwright） |
| 24.4 回归测试套件 | 渲染结果截图对比测试，防止渲染回归 |

---

## 实施路线图建议

```
Phase 1 (MVP-组态核心) ─────────────────────────────────
  ├─ #1  数据绑定系统（1.1-1.4）
  ├─ #2  事件/触发器系统（2.1-2.4）
  ├─ #3  运行时播放器（3.1-3.3）
  ├─ #5  项目管理页面（5.1-5.4）
  ├─ #6  键盘快捷键（6.1-6.2）
  └─ #8  自动保存与崩溃恢复（8.1-8.3）

Phase 2 (编辑器可用性) ─────────────────────────────────
  ├─ #4  2D 叠加层系统（4.1-4.4）
  ├─ #7  对齐与分布工具
  ├─ #9  场景树增强
  ├─ #10 更多 3D 格式（10.1-10.3）
  ├─ #11 材质库与预设
  └─ #12 测量与标注工具

Phase 3 (团队协作) ─────────────────────────────────────
  ├─ #13 实时协作编辑
  ├─ #14 权限与角色管理
  ├─ #15 模板系统
  └─ #16 资产市场/共享库

Phase 4 (高级渲染) ─────────────────────────────────────
  ├─ #17 动画系统
  ├─ #18 高级渲染特性
  ├─ #19 粒子与特效
  └─ #20 物理与碰撞

Phase 5 (生产就绪) ─────────────────────────────────────
  ├─ #21 部署与运维
  ├─ #22 安全加固
  ├─ #23 监控与可观测性
  └─ #24 测试覆盖
```

> 注：Phase 5 的部分子项（如 Docker 化、SECRET_KEY 外置）可根据实际部署需求提前到 Phase 1 同步进行。
